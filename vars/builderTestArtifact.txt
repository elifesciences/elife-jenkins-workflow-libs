Archives the `remoteTestArtifact` in the build result. May be called multiple times.

`stackname` has to be specified at the same time to download the artifact from that remote machine into the workspace `build/` folder.
