Opens a pull request with the current `branch`, after pushing it to origin.

`title` must be less than one line and it's good practice for it to be short. The optional `description` can be as long as you want and can contain Markdown formatting.

You can customize the `temporaryFile` to be used.
