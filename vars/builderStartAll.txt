Executed `builderStart` on all `stacks`.

If starting actions have to be performed, they are run in parallel for better performance.

Each starting action takes a lock with the same name as the stack.
