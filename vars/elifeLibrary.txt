Similar to `elifePipeline`, but works with `elifeOnNode` to execute on a specific Jenkins node.

See `elifeOnNode` for reference on `body` and `nodeName`.

Optionally, specify `timeoutInMinutes` to override the default timeout for very long builds.
