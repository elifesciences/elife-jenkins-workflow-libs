Git-adds the `add` path[s] in the repository, and performs an automated commit with message `message`.

Returns `true` if it actually committed a change, or `false` if there was no change in the diff.
